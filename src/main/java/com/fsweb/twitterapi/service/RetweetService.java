package com.fsweb.twitterapi.service;

import org.springframework.stereotype.Service;

@Service
public class RetweetService {
    // Retweetlerle ilgili iş mantığı buraya gelecek.
    // Örneğin:
    // private final RetweetRepository retweetRepository;
    // private final TweetRepository tweetRepository;
    // private final UserRepository userRepository;

    // public Retweet retweetTweet(UUID tweetId, UUID userId) { ... }
    // public void unretweetTweet(UUID tweetId, UUID userId) { ... }
    // public boolean userRetweetedTweet(UUID tweetId, UUID userId) { ... }
}