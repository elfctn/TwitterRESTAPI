package com.fsweb.twitterapi.service;

import org.springframework.stereotype.Service;

@Service
public class CommentService {
    // Yorumlarla ilgili iş mantığı buraya gelecek.
    // Örneğin:
    // private final CommentRepository commentRepository;
    // private final TweetRepository tweetRepository;
    // private final UserRepository userRepository;

    // public Comment addComment(CommentCreateRequest request, UUID tweetId, UUID userId) { ... }
    // public Comment updateComment(UUID commentId, CommentUpdateRequest request, UUID userId) { ... }
    // public void deleteComment(UUID commentId, UUID userId) { ... } // Hem yorum sahibi hem tweet sahibi silebilir
}